namespace CICDNotas.Services.Sesion
{
    public interface Interface
    {
        string GetToken();
        void SetToken(string token);

        string GetToken();
        void SetToken(string token);
    }
}
